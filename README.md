# WIP: SugarFunge Tests and Examples

## Examples

### Prime
Add 100 classes with 100 assets to each class.
```
cargo run --release --example prime_assets
```

Mints for several accounts
```
cargo run --release --example prime_mints
```